[project]
name = "data360-mcp"
version = "0.1.0"
description = "Data360 MCP Server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.18.0",
]


[tool.setuptools.packages.find]
where = ["data360_mcp"]
